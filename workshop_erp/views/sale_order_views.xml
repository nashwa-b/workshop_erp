<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_sale_order_form_inherit_job_order" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.job.order</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                    <field  name="job_order_id"/>
                     <field name="vehicle_id"/>
                </xpath>
             </field>
    </record>
</odoo>


<!-- invisible="not hide or (status != 'done') or sale_order_id"-->
<!-- invisible="not sale_order_id or status != 'done'"-->
